image: node:18

stages:
  - build
  - test

variables:
  NODE_ENV: 'development'

cache:
  paths:
    - node_modules/

before_script:
  - npm install

build:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 week
  only:
    - master  # 或者指定其他分支